// 测试用例8：continue语句
// 测试目的：验证编译器能否正确处理continue语句

int main() {
    int sum = 0;
    int i = 0;

    while (i < 10) {
        i = i + 1;
        if (i % 2 == 0) {
            continue;  // 跳过偶数
        }
        sum = sum + i;
    }

    return sum;
}
