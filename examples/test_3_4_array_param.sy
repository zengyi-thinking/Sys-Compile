// 测试用例3-4：数组参数测试
// 测试目的：验证函数的数组参数能否正确处理地址传递
int sum(int arr[], int n) {
    int total = 0;
    int i = 0;
    while (i < n) {
        total = total + arr[i];
        i = i + 1;
    }
    return total;
}

int main() {
    int data[5];
    data[0] = 1;
    data[1] = 2;
    int result = sum(data, 5);
    return result;
}
